tmp
.tmp
sessions
session_index.jsonl
log
history.jsonl
auth.json
models_cache
models_cache.json
version
version.json
rules/claude_sync.generated.rules
shell_snapshots
.codex-global-state.json
sqlite/
vendor_imports/
cache/
state_*.sqlite
installation_id
computer-use/
memories/
ambient-suggestions/
process_manager/
app-server-control/
attachments/
chrome-native-hosts-v2.json
.codex-chronicle-assets-to-install.marker
mcp-oauth-locks/
packages/

# Private Codex CLI application state. ~/.codex is a symlink into this PUBLIC
# repo, so anything the CLI writes here lands in the working tree — these must
# never become committable.
agents/
archived_sessions/
context-mode/
generated_images/
hooks/
hooks.json
ipc/
lsp-install-decisions.json
orchestrations/
visualizations/
worktrees/

# SQLite databases and their WAL/SHM sidecars. Sidecars were previously covered
# only by the machine-local ~/.gitignore_global, which is not part of this repo.
*.sqlite
*.sqlite-wal
*.sqlite-shm
